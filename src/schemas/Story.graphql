type Story {
    author: User
    by: String
    commentCount: Int!
    comments(first: Int!): [Comment]
    descendants: Int
    id: Int!
    kids: [Int]
    score: Int
    time: Date
    title: String
    url: String
    type: String
}
